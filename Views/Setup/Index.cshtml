@model Invoice.Net.ViewModels.LoginViewModel

@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@Html.ValidationSummary(true)

@using (Html.BeginForm())
{ 
    @Html.EditorForModel()
    <p>
        <button type="submit" class="btn btn-primary">Login</button>
    </p>
}
